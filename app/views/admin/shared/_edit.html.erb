<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<%= form_tag("/admin/content/merge/#{@article.id}", :id => "merge_form", :enctype => "multipart/form-data") do %>
    <h4><%= _("Merge Articles") %></h4>
    <p>
      Article ID <input type="text" name="merge_id" id="merge_id" name="merge_with" style="width: 25%;" />
    </p>
    <button type="submit">Merge</button>
<% end %>
